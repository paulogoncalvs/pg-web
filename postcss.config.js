// This configuration tells PostCSS to use the specified plugins
export default {
    plugins: {
        // The new PostCSS plugin for Tailwind CSS v4
        '@tailwindcss/postcss': {},
        // Adds vendor prefixes to CSS rules
        autoprefixer: {},
    },
};
