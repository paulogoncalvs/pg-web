services:
  pg-tests-runner:
    build:
      context: .
      dockerfile: Dockerfile.tests
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    environment:
      - NODE_ENV=tests
    volumes:
      - ./:/app
    command: 'yarn playwright test'
